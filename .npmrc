allow-build-scripts=@whiskeysockets/baileys,sharp,esbuild,protobufjs,fs-ext,node-pty,@lydell/node-pty,@matrix-org/matrix-sdk-crypto-nodejs

# ========== 安装加速（pnpm install） ==========
# 并发下载（16→32 显著提速，可按网络调整）
network-concurrency=32
# 优先使用本地缓存
prefer-offline=true
# 国内镜像（国外可注释或 NPM_CONFIG_REGISTRY= 覆盖）
registry=https://registry.npmmirror.com
# 单包超时 2min，避免大包（sharp/playwright）卡死
fetch-timeout=120000
# 重试 2 次，快速失败
fetch-retries=2
# 链接阶段并发
child-concurrency=5
